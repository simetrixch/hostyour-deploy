# Publishing the mail DNS of one sender domain: its SPF, its DKIM key and its DMARC policy, through
# the DNS provider's API.
#
# AN OPERATION THE OPERATOR TRIGGERS, NOT A STEP OF A DEPLOYMENT. It touches a DNS zone that is not
# part of the machine — often the apex of a domain whose mail already runs on somebody else's
# service — so it runs when a person decides it runs, against answers that person gives, and a green
# dry run of the same answers is what admits it like every other program.
#
# ONE RUN PUBLISHES ONE SENDER DOMAIN. An installation that sends as several apexes runs this once
# per apex, with the answers of that apex; there is no domain list in any file, because a list in a
# file is a loop, and a program file may not loop. What every run shares — where the checkout is,
# where the hand-filled input stands, which variable holds the API token — is stated once under
# defaults.
#
# THE TRAP THIS PROGRAM IS BUILT AROUND: a domain may carry exactly ONE v=spf1 record. A second one
# is a permanent error receivers fail the domain's mail on — silently, at their end, for every
# sender the domain has. So the SPF step MERGES into the record it finds, preserving every mechanism
# and the closing qualifier that are already there, and REFUSES the domain wholesale when it finds
# two, naming both. That refusal stands FIRST in this program with on_failure exit, so a domain
# whose SPF is already broken gets no DKIM and no DMARC either — never a partially rewritten mail
# zone on top of a permanent error.
#
# WHAT THIS PROGRAM DOES NOT DO. It writes no address record: the name the sending machine greets
# with is its own name — the one the reverse DNS of the address gives, resolving back to that
# address — and the apex of the sender domain stays free for whatever it serves. The reverse-DNS
# entry is set where the address is rented, not in any DNS zone this token can edit. And no row
# derives a DKIM public key from a private one: the caller that holds the pair signing this domain
# answers its public half; without an answer, the relay's own pair stands in the store (minted by
# deploy-platform-services) and the row below reads its public half.
name: publish-mail-dns
roles: [master]

defaults:
  # The checkout this installation runs from, and under it the ONE hand-filled input every
  # credential of the installation is typed into. The API token already stands there for the
  # cluster manager's own DNS records — this program reads the SAME variable, so the credential
  # has one home and no second copy that can drift.
  repository: /srv/hostyour-cloud
  secrets_path: secrets/secrets.<stage>
  token_variable: CLOUDFLARE_DNS_API_TOKEN
  # Which answer the "<stage>" above stands for. The word is stated once, here, rather than known
  # by the package — a product with regions instead of stages would name that answer and write
  # "<region>".
  run_answer: stage
  # Where the store's profile and root token stand, for the one row that reads the store — the same
  # layout deploy-platform-services writes them under, stated the same way.
  profile_path: clusters/active/<fqdn>.yaml
  domain_answer: fqdn
  vault_url_key: global.endpoints.vault.url
  cluster_name_key: global.clusterName
  kubernetes_auth_path_key: global.vaultKubernetesAuthPath
  credentials_path: secrets/vault-<stage>.txt

steps:
  # SPF FIRST, AND THE ORDER IS THE SAFETY PROPERTY. This is the one row that can find the domain
  # already broken (two v=spf1 records) and refuse it. Standing first with on_failure exit, its
  # refusal stops the run before any other record of the domain is touched — a wholesale refusal for
  # the domain, expressed as order rather than as a per-domain abort list.
  - step: cloudflare_spf_record
    apex_answer: mail_domain
    address_answer: egress_address
    on_failure: exit

  # THE KEY THE RELAY SIGNS WITH, read out of the store. The pair is minted where the store is
  # seeded and the public half stands beside the private one; this row publishes it for the row
  # below. An installation whose store holds no pair is refused here and named — and the run goes
  # on, because the SPF above is right without a signature and the DKIM row below then publishes the
  # answered key, or nothing and says so. The private half is never read by this program.
  - step: measure_dkim_public_key
    mount: secret
    path: <stage>/app/postfix
    public_key_field: dkim-public-key
    on_failure: continue

  # Published ONLY where a signing key exists. The answered key wins: the caller that answers one
  # holds the pair that signs this domain, which is not the relay's. Without an answer the step
  # takes the public half the row above measured; where that row could not (no pair in the store),
  # it falls back to the hand-filled input the token stands in, and with that empty too it publishes
  # nothing and says so — a DKIM record nothing signs with makes receivers fail mail that is
  # otherwise fine.
  - step: cloudflare_dkim_record
    apex_answer: mail_domain
    selector_answer: dkim_selector
    public_key_answer: dkim_public_key
    public_key: {measured: dkim_public_key}
    public_key_variable: MAIL_RELAY_DKIM_PUBLIC_KEY
    on_failure: exit

  - step: cloudflare_dmarc_record
    apex_answer: mail_domain
    policy_answer: dmarc_policy
    mailbox_answer: dmarc_mailbox
    on_failure: exit

answers:
  - name: stage
    kind: text
    allowed: [dev, test, prod]
    describes: >-
      Which of the product's three stages this installation runs. It names the hand-filled input
      the API token and the DKIM public key are read out of, and it is the default DKIM selector.

  - name: mail_domain
    kind: text
    shape: hostname
    describes: >-
      The domain mail is sent AS — the part after the @ of the sender addresses. Its zone must be
      on the DNS account the token can edit. This may be a live mail domain of another service:
      the SPF merge preserves what that service published, and two existing v=spf1 records refuse
      the whole domain rather than guess.

  - name: egress_address
    kind: text
    describes: >-
      The public IPv4 address the mail actually leaves from — the address of the machine the
      sending MTA stands on. It is the ip4 mechanism the SPF merge adds. Answer with the address,
      not a name: an ip4 mechanism names one, and this program resolves nothing.

  - name: dkim_selector
    kind: text
    required: false
    # LEFT EMPTY MEANS THE STAGE. The signer of this installation signs with the stage as its
    # selector, so the value falls back to the stage answer and nobody types the same word into
    # two fields where the pair could disagree. It matters only where a key is published at all.
    default_from: stage
    describes: >-
      The DKIM selector — the label receivers look the key up under, which must be the one the
      signer actually writes into its signatures. Leave it empty to use the stage, which is what
      this installation's signer does.

  - name: dkim_public_key
    kind: text
    required: false
    describes: >-
      The public half of the DKIM key this domain's mail is signed with, as the base64 a DKIM
      record carries in p= — answered by the caller that holds the pair, for example the cluster
      manager for the domain a unit with an SMTP entry sends as. Leave it empty where the relay of
      this installation signs: its key is then read out of the store.

  - name: fqdn
    kind: text
    shape: hostname
    describes: >-
      This cluster's own domain — the name its map stands under, which is where the one row that
      reads the store finds the store's address and its own name.

  - name: dmarc_policy
    kind: text
    allowed: [none, quarantine, reject]
    default: none
    describes: >-
      What receivers do with mail that fails DMARC alignment. Start at none — reports without
      enforcement — and tighten to quarantine or reject once the reports show only your own mail.

  - name: dmarc_mailbox
    kind: text
    shape: mailbox
    describes: >-
      The mailbox receivers send their aggregate DMARC reports to. It is how you learn who is
      sending as this domain, so answer with one somebody actually reads.
